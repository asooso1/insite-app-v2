# Insite App Migration PRD
## Product Requirements Document - 2026.02 Modern Stack Migration

---

## 1. Executive Summary

### Project Overview
Insite App 시설관리 모바일 앱을 2026년 2월 기준 최신 React Native 기술 스택으로 전면 마이그레이션합니다. API 연동은 유지하면서 프론트엔드 아키텍처, UI/UX, 개발자 경험을 현대화합니다.

### Current State (AS-IS)
| 항목 | 현재 상태 |
|------|----------|
| React Native | 0.75.4 |
| Language | JavaScript (No TypeScript) |
| Navigation | React Navigation v6 |
| State Management | Apollo Reactive Variables |
| UI Library | native-base + react-native-paper (혼용) |
| Styling | StyleSheet.create + inline (비일관적) |
| Date Library | moment.js (deprecated) |
| Testing | Jest 설정만 존재 (테스트 없음) |
| Design System | 없음 |
| 시니어 모드 | 폰트 크기만 확대 |

### Target State (TO-BE)
| 항목 | 목표 상태 |
|------|----------|
| React Native | 0.83+ (New Architecture 필수) |
| Expo SDK | 54/55 (Development Build) |
| Language | TypeScript (strict mode) |
| Navigation | Expo Router (file-based) |
| State Management | TanStack Query + Zustand |
| UI Library | Gluestack UI v3 |
| Styling | NativeWind (Tailwind) |
| Date Library | date-fns |
| Testing | Jest 30 + RNTL |
| Design System | Token 기반 디자인 시스템 |
| 시니어 모드 | 완전 별도 UI/UX 디자인 |

---

## 2. Goals & Success Metrics

### Primary Goals

#### G1: 확장성 (Extensibility)
- 새 기능 추가 시간 50% 단축
- Feature 기반 모듈식 아키텍처로 독립적 기능 개발
- 파일 기반 라우팅으로 화면 추가 간소화

#### G2: 생산성 (Productivity)
- TypeScript로 런타임 에러 70% 감소
- 디자인 시스템으로 UI 개발 시간 40% 단축
- 코드 재사용률 60% 이상

#### G3: 안정성 (Reliability)
- 테스트 커버리지 80% 이상
- 타입 안전성 100% (strict TypeScript)
- 에러 모니터링 및 자동 복구

#### G4: 접근성 (Accessibility)
- 60대 이상 사용자를 위한 완전한 시니어 모드
- WCAG 2.1 AA 준수
- 한국어 가독성 최적화

### Success Metrics
| 지표 | 현재 | 목표 |
|------|------|------|
| 빌드 시간 | ~5분 | <2분 |
| 앱 시작 시간 | ~3초 | <1.5초 |
| 타입 커버리지 | 0% | 100% |
| 테스트 커버리지 | 0% | 80% |
| 번들 크기 | ~25MB | <18MB |
| 시니어 모드 만족도 | 측정 없음 | 90%+ |

---

## 3. Project Structure (신규)

### 3.1 전체 구조

```
InsiteApp/
│
├── 📁 app/                      # Expo Router (파일 = 라우트)
│   ├── _layout.tsx              # 루트 레이아웃 (테마 프로바이더)
│   ├── index.tsx                # 엔트리 (리다이렉트)
│   │
│   ├── 📁 (auth)/               # 인증 그룹 (미로그인)
│   │   ├── _layout.tsx
│   │   ├── login.tsx
│   │   ├── guest-login.tsx
│   │   └── password-change.tsx
│   │
│   ├── 📁 (tabs)/               # 메인 탭 네비게이션
│   │   ├── _layout.tsx          # 탭바 설정
│   │   ├── home.tsx
│   │   ├── my-work.tsx
│   │   ├── scan.tsx
│   │   ├── calendar.tsx
│   │   └── settings.tsx
│   │
│   ├── 📁 work/                 # 작업지시
│   │   ├── index.tsx            # /work (목록)
│   │   ├── [id].tsx             # /work/123 (상세)
│   │   ├── create.tsx           # /work/create
│   │   └── 📁 [id]/
│   │       ├── result.tsx       # /work/123/result
│   │       └── check/
│   │           └── [checkId].tsx
│   │
│   ├── 📁 patrol/               # 순찰점검
│   │   ├── worker.tsx
│   │   ├── admin.tsx
│   │   ├── [id].tsx
│   │   ├── scan.tsx
│   │   └── floor-overview.tsx
│   │
│   ├── 📁 dashboard/            # 대시보드
│   │   ├── bems.tsx
│   │   ├── ncp.tsx
│   │   ├── alarm.tsx
│   │   └── operating.tsx
│   │
│   ├── 📁 equipment/            # 설비
│   │   ├── index.tsx
│   │   └── [id].tsx
│   │
│   ├── 📁 trouble/              # 하자/민원
│   │   ├── index.tsx
│   │   ├── [id].tsx
│   │   └── create.tsx
│   │
│   └── 📁 settings/             # 설정
│       ├── profile.tsx
│       ├── favorites.tsx
│       ├── notifications.tsx
│       └── senior-mode.tsx
│
├── 📁 src/
│   │
│   ├── 📁 features/             # ⭐ 기능별 모듈 (핵심)
│   │   │
│   │   ├── 📁 auth/             # 인증 기능
│   │   │   ├── 📁 components/
│   │   │   │   ├── LoginForm.tsx
│   │   │   │   ├── PasswordInput.tsx
│   │   │   │   └── DeviceAuthModal.tsx
│   │   │   ├── 📁 hooks/
│   │   │   │   ├── useLogin.ts
│   │   │   │   ├── useAuth.ts
│   │   │   │   └── useDeviceAuth.ts
│   │   │   ├── 📁 api/
│   │   │   │   ├── auth.api.ts
│   │   │   │   └── auth.types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── 📁 work/             # 작업지시 기능
│   │   │   ├── 📁 components/
│   │   │   │   ├── WorkOrderCard.tsx
│   │   │   │   ├── WorkOrderList.tsx
│   │   │   │   ├── WorkOrderDetail.tsx
│   │   │   │   ├── WorkResultForm.tsx
│   │   │   │   ├── CheckItemCard.tsx
│   │   │   │   └── CheckItemList.tsx
│   │   │   ├── 📁 hooks/
│   │   │   │   ├── useWorkOrders.ts
│   │   │   │   ├── useWorkOrder.ts
│   │   │   │   └── useWorkResult.ts
│   │   │   ├── 📁 api/
│   │   │   │   ├── work.api.ts
│   │   │   │   └── work.types.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── 📁 patrol/           # 순찰점검 기능
│   │   │   ├── 📁 components/
│   │   │   ├── 📁 hooks/
│   │   │   ├── 📁 api/
│   │   │   └── index.ts
│   │   │
│   │   ├── 📁 dashboard/        # 대시보드 기능
│   │   │   ├── 📁 components/
│   │   │   │   ├── BEMSChart.tsx
│   │   │   │   ├── NCPPanel.tsx
│   │   │   │   ├── AlarmCard.tsx
│   │   │   │   └── StatCard.tsx
│   │   │   ├── 📁 hooks/
│   │   │   └── index.ts
│   │   │
│   │   ├── 📁 equipment/        # 설비 기능
│   │   ├── 📁 trouble/          # 하자/민원 기능
│   │   ├── 📁 notification/     # 알림 기능
│   │   └── 📁 scan/             # NFC/QR 스캔 기능
│   │
│   ├── 📁 components/           # ⭐ 공용 UI 컴포넌트
│   │   │
│   │   ├── 📁 ui/               # 기본 UI 컴포넌트
│   │   │   ├── Button/
│   │   │   │   ├── Button.tsx
│   │   │   │   ├── Button.test.tsx
│   │   │   │   └── index.ts
│   │   │   ├── Input/
│   │   │   ├── Card/
│   │   │   ├── Badge/
│   │   │   ├── Avatar/
│   │   │   ├── Icon/
│   │   │   ├── Modal/
│   │   │   └── Toast/
│   │   │
│   │   ├── 📁 layout/           # 레이아웃 컴포넌트
│   │   │   ├── Container.tsx
│   │   │   ├── Stack.tsx
│   │   │   ├── Grid.tsx
│   │   │   ├── Divider.tsx
│   │   │   └── SafeArea.tsx
│   │   │
│   │   ├── 📁 forms/            # 폼 컴포넌트
│   │   │   ├── TextField.tsx
│   │   │   ├── Select.tsx
│   │   │   ├── DatePicker.tsx
│   │   │   ├── Checkbox.tsx
│   │   │   ├── RadioGroup.tsx
│   │   │   └── ImagePicker.tsx
│   │   │
│   │   ├── 📁 feedback/         # 피드백 컴포넌트
│   │   │   ├── Loading.tsx
│   │   │   ├── Empty.tsx
│   │   │   ├── Error.tsx
│   │   │   ├── Skeleton.tsx
│   │   │   └── RefreshControl.tsx
│   │   │
│   │   └── 📁 navigation/       # 네비게이션 컴포넌트
│   │       ├── TabBar.tsx
│   │       ├── Header.tsx
│   │       ├── BackButton.tsx
│   │       └── ScanButton.tsx
│   │
│   ├── 📁 api/                  # ⭐ API 공통 설정
│   │   ├── client.ts            # Axios 인스턴스
│   │   ├── interceptors.ts      # 인터셉터
│   │   ├── error-handler.ts     # 에러 처리
│   │   └── 📁 types/
│   │       └── common.ts        # 공용 타입
│   │
│   ├── 📁 stores/               # ⭐ Zustand 스토어
│   │   ├── auth.store.ts        # 인증 상태
│   │   ├── ui.store.ts          # UI 상태
│   │   ├── settings.store.ts    # 설정 상태
│   │   └── index.ts
│   │
│   ├── 📁 theme/                # ⭐ 디자인 시스템
│   │   ├── tokens/
│   │   │   ├── colors.ts
│   │   │   ├── spacing.ts
│   │   │   ├── typography.ts
│   │   │   └── index.ts
│   │   ├── variants/
│   │   │   ├── default.ts       # 기본 테마
│   │   │   └── senior.ts        # 시니어 테마
│   │   ├── components.ts        # 컴포넌트 스타일
│   │   └── index.ts
│   │
│   ├── 📁 hooks/                # 공용 훅
│   │   ├── useDebounce.ts
│   │   ├── useKeyboard.ts
│   │   ├── useRefresh.ts
│   │   ├── useTheme.ts
│   │   └── useSeniorMode.ts
│   │
│   ├── 📁 utils/                # 유틸리티
│   │   ├── format.ts            # 날짜, 숫자 포맷
│   │   ├── validation.ts        # 유효성 검사
│   │   ├── cn.ts                # className 유틸
│   │   └── storage.ts           # 저장소 유틸
│   │
│   ├── 📁 constants/            # 상수
│   │   ├── environment.ts
│   │   ├── routes.ts
│   │   └── config.ts
│   │
│   └── 📁 types/                # 전역 타입
│       ├── navigation.ts
│       ├── env.d.ts
│       └── global.d.ts
│
├── 📁 assets/                   # 정적 자산
│   ├── 📁 images/
│   ├── 📁 fonts/
│   └── 📁 icons/
│
├── 📁 __tests__/                # 테스트
│   ├── 📁 unit/
│   ├── 📁 integration/
│   └── 📁 e2e/
│
├── app.config.ts                # Expo 설정
├── tailwind.config.js           # Tailwind 설정
├── tsconfig.json                # TypeScript 설정
├── eas.json                     # EAS Build 설정
└── package.json
```

### 3.2 Import 경로 별칭

```json
// tsconfig.json paths
{
  "@/*": ["src/*"],
  "@/components/*": ["src/components/*"],
  "@/features/*": ["src/features/*"],
  "@/api/*": ["src/api/*"],
  "@/stores/*": ["src/stores/*"],
  "@/theme/*": ["src/theme/*"],
  "@/hooks/*": ["src/hooks/*"],
  "@/utils/*": ["src/utils/*"],
  "@/constants/*": ["src/constants/*"],
  "@/types/*": ["src/types/*"]
}
```

### 3.3 Feature 모듈 규칙

```typescript
// features/work/index.ts - 배럴 파일

// Components
export { WorkOrderCard } from './components/WorkOrderCard';
export { WorkOrderList } from './components/WorkOrderList';
export { WorkOrderDetail } from './components/WorkOrderDetail';

// Hooks
export { useWorkOrders } from './hooks/useWorkOrders';
export { useWorkOrder } from './hooks/useWorkOrder';

// Types
export type { WorkOrder, WorkOrderInput } from './api/work.types';
```

```typescript
// 사용 예시
import {
  WorkOrderCard,
  useWorkOrders,
  type WorkOrder
} from '@/features/work';
```

---

## 4. 시니어 모드 디자인 시스템 (신규)

### 4.1 시니어 모드 개요

**대상:** 60대 이상 한국인 시설관리 근로자

**핵심 원칙:**
1. **큰 글씨** - 최소 18px, 기본 20px
2. **높은 대비** - 7:1 이상 색상 대비
3. **단순한 구조** - 한 화면에 핵심 기능만
4. **넓은 터치 영역** - 최소 56px
5. **명확한 피드백** - 큰 토스트, 진동

### 4.2 시니어 모드 디자인 토큰

```typescript
// src/theme/variants/senior.ts

export const seniorTheme = {
  // ============================================
  // 타이포그래피 - 큰 글씨
  // ============================================
  typography: {
    fontSize: {
      xs: 16,      // 일반: 12
      sm: 18,      // 일반: 14
      md: 20,      // 일반: 16 (기본)
      lg: 24,      // 일반: 18
      xl: 28,      // 일반: 20
      '2xl': 32,   // 일반: 24
      '3xl': 40,   // 일반: 30
    },
    fontWeight: {
      normal: '500',   // 더 굵게
      medium: '600',
      semibold: '700',
      bold: '800',
    },
    lineHeight: {
      tight: 1.4,
      normal: 1.6,     // 더 여유있게
      relaxed: 1.8,
    },
  },

  // ============================================
  // 색상 - 고대비
  // ============================================
  colors: {
    // 텍스트 (더 진하게)
    text: {
      primary: '#000000',      // 순수 검정
      secondary: '#333333',    // 진한 회색
      tertiary: '#555555',
      inverse: '#FFFFFF',
    },

    // 배경 (더 밝게)
    background: {
      default: '#FFFFFF',
      paper: '#F8F8F8',
      elevated: '#FFFFFF',
    },

    // 브랜드 (더 진하게)
    primary: {
      main: '#0D47A1',         // 더 진한 파랑
      light: '#1976D2',
      dark: '#002171',
      contrast: '#FFFFFF',
    },

    // 시맨틱 (더 선명하게)
    success: {
      main: '#1B5E20',         // 진한 초록
      light: '#4CAF50',
      contrast: '#FFFFFF',
    },
    warning: {
      main: '#E65100',         // 진한 주황
      light: '#FF9800',
      contrast: '#000000',
    },
    error: {
      main: '#B71C1C',         // 진한 빨강
      light: '#F44336',
      contrast: '#FFFFFF',
    },

    // 테두리 (더 진하게)
    border: {
      default: '#BDBDBD',
      focus: '#0D47A1',
    },
  },

  // ============================================
  // 간격 - 더 넓게
  // ============================================
  spacing: {
    xs: 8,       // 일반: 4
    sm: 12,      // 일반: 8
    md: 20,      // 일반: 16
    lg: 28,      // 일반: 24
    xl: 40,      // 일반: 32
    '2xl': 56,   // 일반: 48
  },

  // ============================================
  // 터치 영역 - 더 크게
  // ============================================
  touch: {
    minHeight: 56,             // 일반: 44
    minWidth: 56,
    buttonHeight: {
      sm: 52,                  // 일반: 36
      md: 60,                  // 일반: 44
      lg: 72,                  // 일반: 56
    },
    iconSize: {
      sm: 28,                  // 일반: 20
      md: 32,                  // 일반: 24
      lg: 40,                  // 일반: 32
    },
  },

  // ============================================
  // 테두리 - 더 두껍게
  // ============================================
  border: {
    width: {
      default: 2,              // 일반: 1
      thick: 3,
    },
    radius: {
      sm: 8,                   // 일반: 4
      md: 12,                  // 일반: 8
      lg: 16,                  // 일반: 12
    },
  },

  // ============================================
  // 애니메이션 - 느리게
  // ============================================
  animation: {
    duration: {
      fast: 200,               // 일반: 100
      normal: 400,             // 일반: 200
      slow: 600,               // 일반: 300
    },
  },
} as const;
```

### 4.3 시니어 모드 컴포넌트 변형

#### Button (시니어)

```typescript
// 일반 모드
<Button size="md">확인</Button>
// → height: 44px, fontSize: 16px

// 시니어 모드 (자동 적용)
<Button size="md">확인</Button>
// → height: 60px, fontSize: 20px, fontWeight: 700
```

```typescript
// src/components/ui/Button/Button.tsx

export function Button({ children, size = 'md', ...props }: ButtonProps) {
  const { isSeniorMode } = useSeniorMode();

  const height = isSeniorMode
    ? seniorTheme.touch.buttonHeight[size]
    : defaultTheme.touch.buttonHeight[size];

  const fontSize = isSeniorMode
    ? seniorTheme.typography.fontSize.md
    : defaultTheme.typography.fontSize.md;

  return (
    <Pressable
      className={cn(
        'items-center justify-center rounded-lg',
        isSeniorMode ? 'border-2' : 'border',
      )}
      style={{ height, minWidth: height }}
      {...props}
    >
      <Text
        style={{ fontSize, fontWeight: isSeniorMode ? '700' : '600' }}
        className="text-white"
      >
        {children}
      </Text>
    </Pressable>
  );
}
```

#### Card (시니어)

```
┌─────────────────────────────────────────┐
│  일반 모드                               │
├─────────────────────────────────────────┤
│  작업지시 #1234                          │
│  2층 화장실 점검                          │
│  담당: 김철수 | 마감: 2/5               │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  시니어 모드                             │
├─────────────────────────────────────────┤
│                                         │
│  📋 작업지시 #1234                       │
│                                         │
│  2층 화장실 점검                          │
│                                         │
│  👤 담당: 김철수                         │
│  📅 마감: 2월 5일 (수)                   │
│                                         │
│  ┌─────────┐  ┌─────────┐              │
│  │  상세   │  │  완료   │              │
│  └─────────┘  └─────────┘              │
│                                         │
└─────────────────────────────────────────┘
```

### 4.4 시니어 모드 네비게이션

#### 일반 모드 탭바

```
┌─────┬─────┬─────┬─────┬─────┐
│ 🏠  │ 📋  │ 📷  │ 📅  │ ⚙️  │
│ 홈  │업무 │스캔 │일정 │설정 │
└─────┴─────┴─────┴─────┴─────┘
```

#### 시니어 모드 탭바

```
┌───────────┬───────────┬───────────┐
│    🏠     │    📋     │    ⚙️     │
│           │           │           │
│    홈     │   내 업무  │   설정    │
└───────────┴───────────┴───────────┘

* 3개로 단순화
* 스캔은 홈에서 큰 버튼으로
* 일정은 홈에서 접근
```

### 4.5 시니어 모드 홈 화면

```
┌─────────────────────────────────────────┐
│  ☀️ 좋은 아침이에요, 김철수님            │
│  오늘 할 일 3건                          │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │                                 │   │
│  │      📷 QR/NFC 스캔하기         │   │
│  │                                 │   │
│  │      (크게 눌러주세요)           │   │
│  │                                 │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  📋 오늘 할 일                   │   │
│  ├─────────────────────────────────┤   │
│  │                                 │   │
│  │  ✅ 1층 로비 점검 (완료)         │   │
│  │                                 │   │
│  │  ⏳ 2층 화장실 점검              │   │
│  │      → 눌러서 시작하기           │   │
│  │                                 │   │
│  │  ⏳ 3층 사무실 점검              │   │
│  │      → 눌러서 시작하기           │   │
│  │                                 │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────┐  ┌─────────────┐      │
│  │  📅 일정    │  │  🔔 알림    │      │
│  │   보기     │  │   3개      │      │
│  └─────────────┘  └─────────────┘      │
│                                         │
└─────────────────────────────────────────┘
```

### 4.6 시니어 모드 작업 상세

```
┌─────────────────────────────────────────┐
│  ← 뒤로                          ✕ 닫기 │
├─────────────────────────────────────────┤
│                                         │
│  📋 작업지시                            │
│                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                         │
│  2층 화장실 점검                         │
│                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                         │
│  📍 위치                                │
│  본관 2층 남자 화장실                    │
│                                         │
│  📅 마감일                              │
│  2월 5일 (수) 오후 6시                  │
│                                         │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                         │
│  📝 점검 항목 (3개)                      │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  □ 세면대 수압 확인              │   │
│  │     → 눌러서 완료하기            │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  □ 변기 상태 확인               │   │
│  │     → 눌러서 완료하기            │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  ☑ 청소 상태 확인 (완료)         │   │
│  └─────────────────────────────────┘   │
│                                         │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │                                 │   │
│  │      ✅ 작업 완료하기            │   │
│  │                                 │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

### 4.7 시니어 모드 피드백

```typescript
// 시니어 모드 피드백 강화

// 1. 토스트 메시지 - 더 크고 오래
const showSeniorToast = (message: string) => {
  Toast.show({
    text: message,
    duration: 5000,        // 일반: 2000
    position: 'center',    // 일반: bottom
    style: {
      fontSize: 24,
      padding: 24,
      borderRadius: 16,
    },
  });
};

// 2. 진동 피드백
const hapticFeedback = () => {
  if (isSeniorMode) {
    Haptics.notificationAsync(
      Haptics.NotificationFeedbackType.Success
    );
  }
};

// 3. 로딩 인디케이터 - 더 크게
<Loading
  size={isSeniorMode ? 80 : 40}
  text={isSeniorMode ? "잠시만 기다려주세요..." : undefined}
/>

// 4. 에러 메시지 - 더 친절하게
const errorMessages = {
  default: {
    network: '네트워크 오류',
  },
  senior: {
    network: '인터넷 연결이 끊어졌어요.\n와이파이를 확인해주세요.',
  },
};
```

### 4.8 시니어 모드 전환

```typescript
// src/stores/settings.store.ts

interface SettingsState {
  isSeniorMode: boolean;
  toggleSeniorMode: () => void;
}

// 시니어 모드 전환 화면
// app/settings/senior-mode.tsx

export default function SeniorModeScreen() {
  const { isSeniorMode, toggleSeniorMode } = useSettingsStore();

  return (
    <Container>
      <Card>
        <Text className="text-xl font-bold mb-4">
          큰 글씨 모드
        </Text>

        <Text className="text-neutral-600 mb-6">
          글씨와 버튼이 더 크게 보여요.{'\n'}
          60세 이상 사용자에게 추천해요.
        </Text>

        <View className="flex-row items-center justify-between">
          <Text className="text-lg">
            {isSeniorMode ? '사용 중' : '사용 안 함'}
          </Text>

          <Switch
            value={isSeniorMode}
            onValueChange={toggleSeniorMode}
            trackColor={{ true: '#0D47A1' }}
            style={{ transform: [{ scale: 1.3 }] }}
          />
        </View>
      </Card>

      {/* 미리보기 */}
      <Card className="mt-4">
        <Text className="text-lg font-bold mb-4">미리보기</Text>

        <View
          style={{
            padding: isSeniorMode ? 20 : 16,
            backgroundColor: '#F5F5F5',
            borderRadius: 12,
          }}
        >
          <Text
            style={{
              fontSize: isSeniorMode ? 20 : 16,
              fontWeight: isSeniorMode ? '700' : '400',
            }}
          >
            이렇게 보여요
          </Text>

          <Button
            className="mt-4"
            style={{
              height: isSeniorMode ? 60 : 44,
            }}
          >
            <Text style={{ fontSize: isSeniorMode ? 20 : 16 }}>
              확인 버튼
            </Text>
          </Button>
        </View>
      </Card>
    </Container>
  );
}
```

### 4.9 시니어 모드 vs 일반 모드 비교표

| 항목 | 일반 모드 | 시니어 모드 |
|------|----------|------------|
| **기본 폰트** | 16px | 20px |
| **최소 폰트** | 12px | 16px |
| **버튼 높이** | 44px | 60px |
| **터치 영역** | 44px | 56px |
| **아이콘 크기** | 24px | 32px |
| **간격** | 16px | 20px |
| **테두리** | 1px | 2px |
| **탭 개수** | 5개 | 3개 |
| **색상 대비** | 4.5:1 | 7:1 |
| **토스트 지속** | 2초 | 5초 |
| **애니메이션** | 200ms | 400ms |

---

## 5. Technical Requirements

### 5.1 Core Framework
```
react-native: ^0.83.0
expo: ~54.0.0
expo-dev-client: ~5.0.0
react: ^18.3.0
typescript: ^5.4.0
```

### 5.2 State Management

#### Server State: TanStack Query
```typescript
@tanstack/react-query: ^5.0.0
```

#### Client State: Zustand
```typescript
zustand: ^5.0.0
```

### 5.3 UI Framework

#### Gluestack UI v3 + NativeWind
```
@gluestack-ui/themed: ^2.0.0
nativewind: ^4.0.0
tailwindcss: ^3.4.0
```

### 5.4 Testing
```
jest: ^30.0.0
@testing-library/react-native: ^12.0.0
detox: ^20.0.0 (E2E)
```

---

## 6. Migration Constraints

### Must Keep (유지 필수)
- 모든 API 엔드포인트 및 연동 로직
- Firebase 푸시 알림
- NFC/QR 스캔 기능
- App Guard 보안 기능
- Microsoft Clarity 분석
- Hot Update 기능 (→ EAS Update로 대체)
- Package Name / Bundle ID: `com.hdclabs.mcsp`
- Keystore / 인증서 (기존 것 사용)

### Can Change (변경 가능)
- UI 컴포넌트 전체
- 상태 관리 방식
- 네비게이션 구조
- 스타일링 방식
- 폴더 구조
- moment.js → date-fns
- native-base/paper → Gluestack UI

---

## 7. Timeline Overview

| Phase | 기간 | 주요 작업 |
|-------|------|----------|
| **Phase 1** | 2주 | Foundation (Expo, TS, 구조, 토큰) |
| **Phase 2** | 4주 | Core (Navigation, State, API) |
| **Phase 3** | 4주 | UI (컴포넌트, 시니어 모드) |
| **Phase 4** | 3주 | Features (기능별 마이그레이션) |
| **Phase 5** | 2주 | QA & Launch |

**Total: ~15주 (약 4개월)**

---

## 8. Appendix

### A. 폰트 추천 (시니어 모드)

| 폰트 | 특징 | 권장 용도 |
|------|------|----------|
| **Pretendard** | 한글 가독성 최고 | 기본 폰트 |
| **Noto Sans KR** | 무난한 한글 | 대안 |
| **KoPubWorld돋움체** | 공공기관 표준 | 공식 문서 |

### B. 색상 접근성 검증

```
시니어 모드 색상 대비 (WCAG AAA 기준: 7:1)

배경 #FFFFFF + 텍스트 #000000 = 21:1 ✅
배경 #FFFFFF + Primary #0D47A1 = 9.4:1 ✅
배경 #FFFFFF + Success #1B5E20 = 8.2:1 ✅
배경 #FFFFFF + Error #B71C1C = 7.8:1 ✅
```

### C. Reference

- [React Native New Architecture](https://reactnative.dev/architecture/landing-page)
- [Expo Router](https://docs.expo.dev/router/introduction/)
- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [Korean Typography Guidelines](https://brunch.co.kr/@thinkaboutlove/259)

---

*Document Version: 2.0*
*Updated: 2026-02-04*
*Changes: 프로젝트 구조 개선, 시니어 모드 디자인 시스템 추가*
